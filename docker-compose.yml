services:
  backend:
    image: internal-office-backend:latest
    container_name: internal-office-backend
    restart: always
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
